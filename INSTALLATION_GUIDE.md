# 📘 دليل التثبيت والتشغيل

## نظام إدارة العلاوات والترفيعات الوظيفية

---

## 📋 المتطلبات الأساسية

قبل البدء، تأكد من توفر المتطلبات التالية على جهازك:

### 1. Python
- **الإصدار المطلوب:** Python 3.8 أو أحدث
- **التحقق من الإصدار:**
  ```bash
  python --version
  # أو
  python3 --version
  ```
- **التثبيت:** قم بتحميل Python من [python.org](https://www.python.org/downloads/)

### 2. pip (مدير الحزم)
- يأتي مثبتاً مع Python بشكل افتراضي
- **التحقق:**
  ```bash
  pip --version
  ```

---

## 🚀 خطوات التثبيت

### الخطوة 1: تحميل الكود من GitHub

```bash
# استنساخ المستودع
git clone https://github.com/shaimaamustafa1994-alt/project2.git

# الانتقال إلى مجلد المشروع
cd project2

# التبديل إلى الفرع الصحيح
git checkout codegen-bot/employee-management-system-complete
```

### الخطوة 2: تثبيت المكتبات المطلوبة

```bash
# تثبيت جميع المتطلبات
pip install -r requirements.txt

# أو على بعض الأنظمة
pip3 install -r requirements.txt
```

#### المكتبات المطلوبة:
- **PyQt5** - للواجهة الرسومية
- **python-dateutil** - للحسابات الزمنية
- **openpyxl** - لتصدير Excel (اختياري)
- **python-docx** - لتصدير Word (اختياري)

### الخطوة 3: التحقق من التثبيت

```bash
# التحقق من تثبيت PyQt5
python -c "import PyQt5; print('PyQt5 installed successfully!')"
```

---

## ▶️ تشغيل التطبيق

### الطريقة الأولى: التشغيل المباشر
```bash
python main.py
```

### الطريقة الثانية: باستخدام python3
```bash
python3 main.py
```

### الطريقة الثالثة (Windows): النقر المزدوج
- انقر نقراً مزدوجاً على ملف `main.py`
- أو قم بإنشاء ملف اختصار

---

## 🗂️ هيكل المشروع

بعد التثبيت، ستجد الهيكل التالي:

```
project2/
├── main.py                          # ملف التشغيل الرئيسي
├── requirements.txt                 # قائمة المكتبات المطلوبة
├── README.md                        # دليل شامل للنظام
├── INSTALLATION_GUIDE.md            # هذا الدليل
├── database/
│   ├── __init__.py
│   ├── db_manager.py               # مدير قاعدة البيانات
│   └── employee_management.db      # قاعدة البيانات (تُنشأ تلقائياً)
├── core/
│   ├── __init__.py
│   └── calculation_engine.py       # محرك الحسابات الذكي
└── ui/
    ├── __init__.py
    ├── main_window.py               # النافذة الرئيسية
    ├── employee_management.py       # إدارة الموظفين
    ├── employees_list.py            # قائمة الموظفين
    ├── employee_profile.py          # ملف الموظف
    ├── events_management.py         # إدارة الأحداث
    ├── entitlements_dashboard.py    # لوحة الاستحقاقات
    └── statistics_dashboard.py      # لوحة الإحصائيات
```

---

## 🔧 حل المشاكل الشائعة

### المشكلة 1: خطأ في استيراد PyQt5
**الحل:**
```bash
pip uninstall PyQt5 PyQt5-sip
pip install PyQt5
```

### المشكلة 2: لا تظهر النوافذ على نظام Linux
**الحل:**
```bash
# تثبيت مكتبات إضافية
sudo apt-get install python3-pyqt5
```

### المشكلة 3: خطأ في ترميز الأحرف العربية
**الحل:**
- تأكد من أن نظامك يدعم UTF-8
- على Windows، قم بتشغيل:
  ```bash
  chcp 65001
  ```

### المشكلة 4: خطأ في إنشاء قاعدة البيانات
**الحل:**
- تأكد من وجود صلاحيات الكتابة في مجلد المشروع
- قم بحذف ملف `employee_management.db` إن وُجد وأعد التشغيل

---

## 📝 أول خطوات بعد التشغيل

### 1. إضافة أول موظف
1. اضغط على "إضافة موظف جديد" من القائمة الجانبية
2. املأ جميع البيانات المطلوبة
3. اضغط على "حفظ الموظف"

### 2. عرض قائمة الموظفين
1. اضغط على "قائمة الموظفين"
2. سترى قائمة بجميع الموظفين المضافين

### 3. إضافة حدث مهني
1. اضغط على "إدارة الأحداث المهنية"
2. اختر الموظف من القائمة
3. حدد نوع الحدث وتفاصيله
4. اضغط "حفظ الحدث"

### 4. متابعة الاستحقاقات
1. اضغط على "لوحة الاستحقاقات"
2. شاهد الاستحقاقات القادمة للأسبوع والشهر

---

## 💾 النسخ الاحتياطي

للحفاظ على بياناتك، قم بعمل نسخة احتياطية دورية لملف قاعدة البيانات:

```bash
# نسخ قاعدة البيانات
cp employee_management.db employee_management_backup_$(date +%Y%m%d).db
```

---

## 🔄 التحديثات

للحصول على آخر التحديثات:

```bash
# جلب آخر التحديثات
git pull origin codegen-bot/employee-management-system-complete

# إعادة تثبيت المتطلبات
pip install -r requirements.txt --upgrade
```

---

## 📞 الدعم الفني

إذا واجهت أي مشاكل:
1. تحقق من قسم "حل المشاكل الشائعة" أعلاه
2. تأكد من تثبيت جميع المتطلبات بشكل صحيح
3. راجع ملف `README.md` للمزيد من المعلومات

---

## ✅ قائمة التحقق من التثبيت الناجح

- [ ] Python 3.8+ مثبت
- [ ] تم استنساخ المستودع بنجاح
- [ ] تم تثبيت جميع المكتبات المطلوبة
- [ ] يعمل التطبيق بدون أخطاء
- [ ] تظهر الواجهة بشكل صحيح
- [ ] تم إنشاء قاعدة البيانات تلقائياً
- [ ] يمكن إضافة موظف جديد
- [ ] تعمل جميع القوائم والأزرار

---

## 🎉 مبروك!

إذا اكتملت جميع الخطوات بنجاح، فأنت الآن جاهز لاستخدام نظام إدارة العلاوات والترفيعات الوظيفية!

**استمتع باستخدام النظام!** 🚀

---

*آخر تحديث: 2024*

